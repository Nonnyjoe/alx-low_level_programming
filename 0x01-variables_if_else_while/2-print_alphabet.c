#include <stdio.h>
/**
 * main - Entry point
 * Description: print all the alphabets in small caps
 * Result: return alphabets
 */
int main(void)
{

char ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
return (0);
}
